#include<stdio.h>
int main()
{
	int Max;
	int cnt = 0;
	int a[9];
	int i;
	for (i = 0; i < 9; i++)
	{
		scanf_s("%d", &a[i]);
	}
	Max = a[0];
	for (i = 0; i < 9; i++)
	{
		if (Max < a[i])
		{
			Max = a[i];
			cnt = i;
		}
	}
	printf("%d\n", Max);
	printf("%d", cnt + 1);

	return 0;
}
